/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type Option,
  type OptionOrNullable,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from 'gill';
import { SOLCHAT_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const SEND_MESSAGE_DISCRIMINATOR = new Uint8Array([
  57, 40, 34, 178, 189, 10, 65, 26,
]);

export function getSendMessageDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SEND_MESSAGE_DISCRIMINATOR
  );
}

export type SendMessageInstruction<
  TProgram extends string = typeof SOLCHAT_PROGRAM_ADDRESS,
  TAccountAuthor extends string | AccountMeta<string> = string,
  TAccountParent extends string | AccountMeta<string> = string,
  TAccountMessageMetadata extends string | AccountMeta<string> = string,
  TAccountMessage extends string | AccountMeta<string> = string,
  TAccountChannelMember extends string | AccountMeta<string> = string,
  TAccountReplyToMessage extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthor extends string
        ? WritableSignerAccount<TAccountAuthor> &
            AccountSignerMeta<TAccountAuthor>
        : TAccountAuthor,
      TAccountParent extends string
        ? ReadonlyAccount<TAccountParent>
        : TAccountParent,
      TAccountMessageMetadata extends string
        ? WritableAccount<TAccountMessageMetadata>
        : TAccountMessageMetadata,
      TAccountMessage extends string
        ? WritableAccount<TAccountMessage>
        : TAccountMessage,
      TAccountChannelMember extends string
        ? ReadonlyAccount<TAccountChannelMember>
        : TAccountChannelMember,
      TAccountReplyToMessage extends string
        ? ReadonlyAccount<TAccountReplyToMessage>
        : TAccountReplyToMessage,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type SendMessageInstructionData = {
  discriminator: ReadonlyUint8Array;
  messageContent: string;
  replyToMessageId: Option<number>;
};

export type SendMessageInstructionDataArgs = {
  messageContent: string;
  replyToMessageId: OptionOrNullable<number>;
};

export function getSendMessageInstructionDataEncoder(): Encoder<SendMessageInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      [
        'messageContent',
        addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder()),
      ],
      ['replyToMessageId', getOptionEncoder(getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: SEND_MESSAGE_DISCRIMINATOR })
  );
}

export function getSendMessageInstructionDataDecoder(): Decoder<SendMessageInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['messageContent', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ['replyToMessageId', getOptionDecoder(getU32Decoder())],
  ]);
}

export function getSendMessageInstructionDataCodec(): Codec<
  SendMessageInstructionDataArgs,
  SendMessageInstructionData
> {
  return combineCodec(
    getSendMessageInstructionDataEncoder(),
    getSendMessageInstructionDataDecoder()
  );
}

export type SendMessageAsyncInput<
  TAccountAuthor extends string = string,
  TAccountParent extends string = string,
  TAccountMessageMetadata extends string = string,
  TAccountMessage extends string = string,
  TAccountChannelMember extends string = string,
  TAccountReplyToMessage extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  author: TransactionSigner<TAccountAuthor>;
  parent: Address<TAccountParent>;
  messageMetadata?: Address<TAccountMessageMetadata>;
  message: Address<TAccountMessage>;
  channelMember?: Address<TAccountChannelMember>;
  replyToMessage?: Address<TAccountReplyToMessage>;
  systemProgram?: Address<TAccountSystemProgram>;
  messageContent: SendMessageInstructionDataArgs['messageContent'];
  replyToMessageId: SendMessageInstructionDataArgs['replyToMessageId'];
};

export async function getSendMessageInstructionAsync<
  TAccountAuthor extends string,
  TAccountParent extends string,
  TAccountMessageMetadata extends string,
  TAccountMessage extends string,
  TAccountChannelMember extends string,
  TAccountReplyToMessage extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof SOLCHAT_PROGRAM_ADDRESS,
>(
  input: SendMessageAsyncInput<
    TAccountAuthor,
    TAccountParent,
    TAccountMessageMetadata,
    TAccountMessage,
    TAccountChannelMember,
    TAccountReplyToMessage,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  SendMessageInstruction<
    TProgramAddress,
    TAccountAuthor,
    TAccountParent,
    TAccountMessageMetadata,
    TAccountMessage,
    TAccountChannelMember,
    TAccountReplyToMessage,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLCHAT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    author: { value: input.author ?? null, isWritable: true },
    parent: { value: input.parent ?? null, isWritable: false },
    messageMetadata: { value: input.messageMetadata ?? null, isWritable: true },
    message: { value: input.message ?? null, isWritable: true },
    channelMember: { value: input.channelMember ?? null, isWritable: false },
    replyToMessage: { value: input.replyToMessage ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.messageMetadata.value) {
    accounts.messageMetadata.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([95, 77, 67])),
        getAddressEncoder().encode(expectAddress(accounts.parent.value)),
      ],
    });
  }
  if (!accounts.channelMember.value) {
    accounts.channelMember.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([95, 77, 69, 77, 66, 69, 82])),
        getAddressEncoder().encode(expectAddress(accounts.parent.value)),
        getAddressEncoder().encode(expectAddress(accounts.author.value)),
      ],
    });
  }
  if (!accounts.replyToMessage.value) {
    accounts.replyToMessage.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([95, 77, 69, 83, 83, 65, 71, 69])
        ),
        getAddressEncoder().encode(expectAddress(accounts.parent.value)),
        getOptionEncoder(getU32Encoder()).encode(
          expectSome(args.replyToMessageId)
        ),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.author),
      getAccountMeta(accounts.parent),
      getAccountMeta(accounts.messageMetadata),
      getAccountMeta(accounts.message),
      getAccountMeta(accounts.channelMember),
      getAccountMeta(accounts.replyToMessage),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getSendMessageInstructionDataEncoder().encode(
      args as SendMessageInstructionDataArgs
    ),
    programAddress,
  } as SendMessageInstruction<
    TProgramAddress,
    TAccountAuthor,
    TAccountParent,
    TAccountMessageMetadata,
    TAccountMessage,
    TAccountChannelMember,
    TAccountReplyToMessage,
    TAccountSystemProgram
  >);
}

export type SendMessageInput<
  TAccountAuthor extends string = string,
  TAccountParent extends string = string,
  TAccountMessageMetadata extends string = string,
  TAccountMessage extends string = string,
  TAccountChannelMember extends string = string,
  TAccountReplyToMessage extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  author: TransactionSigner<TAccountAuthor>;
  parent: Address<TAccountParent>;
  messageMetadata: Address<TAccountMessageMetadata>;
  message: Address<TAccountMessage>;
  channelMember?: Address<TAccountChannelMember>;
  replyToMessage?: Address<TAccountReplyToMessage>;
  systemProgram?: Address<TAccountSystemProgram>;
  messageContent: SendMessageInstructionDataArgs['messageContent'];
  replyToMessageId: SendMessageInstructionDataArgs['replyToMessageId'];
};

export function getSendMessageInstruction<
  TAccountAuthor extends string,
  TAccountParent extends string,
  TAccountMessageMetadata extends string,
  TAccountMessage extends string,
  TAccountChannelMember extends string,
  TAccountReplyToMessage extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof SOLCHAT_PROGRAM_ADDRESS,
>(
  input: SendMessageInput<
    TAccountAuthor,
    TAccountParent,
    TAccountMessageMetadata,
    TAccountMessage,
    TAccountChannelMember,
    TAccountReplyToMessage,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): SendMessageInstruction<
  TProgramAddress,
  TAccountAuthor,
  TAccountParent,
  TAccountMessageMetadata,
  TAccountMessage,
  TAccountChannelMember,
  TAccountReplyToMessage,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLCHAT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    author: { value: input.author ?? null, isWritable: true },
    parent: { value: input.parent ?? null, isWritable: false },
    messageMetadata: { value: input.messageMetadata ?? null, isWritable: true },
    message: { value: input.message ?? null, isWritable: true },
    channelMember: { value: input.channelMember ?? null, isWritable: false },
    replyToMessage: { value: input.replyToMessage ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.author),
      getAccountMeta(accounts.parent),
      getAccountMeta(accounts.messageMetadata),
      getAccountMeta(accounts.message),
      getAccountMeta(accounts.channelMember),
      getAccountMeta(accounts.replyToMessage),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getSendMessageInstructionDataEncoder().encode(
      args as SendMessageInstructionDataArgs
    ),
    programAddress,
  } as SendMessageInstruction<
    TProgramAddress,
    TAccountAuthor,
    TAccountParent,
    TAccountMessageMetadata,
    TAccountMessage,
    TAccountChannelMember,
    TAccountReplyToMessage,
    TAccountSystemProgram
  >);
}

export type ParsedSendMessageInstruction<
  TProgram extends string = typeof SOLCHAT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    author: TAccountMetas[0];
    parent: TAccountMetas[1];
    messageMetadata: TAccountMetas[2];
    message: TAccountMetas[3];
    channelMember?: TAccountMetas[4] | undefined;
    replyToMessage?: TAccountMetas[5] | undefined;
    systemProgram: TAccountMetas[6];
  };
  data: SendMessageInstructionData;
};

export function parseSendMessageInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedSendMessageInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 7) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === SOLCHAT_PROGRAM_ADDRESS
      ? undefined
      : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      author: getNextAccount(),
      parent: getNextAccount(),
      messageMetadata: getNextAccount(),
      message: getNextAccount(),
      channelMember: getNextOptionalAccount(),
      replyToMessage: getNextOptionalAccount(),
      systemProgram: getNextAccount(),
    },
    data: getSendMessageInstructionDataDecoder().decode(instruction.data),
  };
}
