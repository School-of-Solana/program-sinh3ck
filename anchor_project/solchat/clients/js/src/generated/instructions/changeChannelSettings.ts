/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from 'gill';
import { SOLCHAT_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const CHANGE_CHANNEL_SETTINGS_DISCRIMINATOR = new Uint8Array([
  51, 33, 5, 116, 154, 144, 72, 121,
]);

export function getChangeChannelSettingsDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CHANGE_CHANNEL_SETTINGS_DISCRIMINATOR
  );
}

export type ChangeChannelSettingsInstruction<
  TProgram extends string = typeof SOLCHAT_PROGRAM_ADDRESS,
  TAccountChannelAuthority extends string | AccountMeta<string> = string,
  TAccountChannel extends string | AccountMeta<string> = string,
  TAccountChannelMember extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountChannelAuthority extends string
        ? ReadonlySignerAccount<TAccountChannelAuthority> &
            AccountSignerMeta<TAccountChannelAuthority>
        : TAccountChannelAuthority,
      TAccountChannel extends string
        ? WritableAccount<TAccountChannel>
        : TAccountChannel,
      TAccountChannelMember extends string
        ? ReadonlyAccount<TAccountChannelMember>
        : TAccountChannelMember,
      ...TRemainingAccounts,
    ]
  >;

export type ChangeChannelSettingsInstructionData = {
  discriminator: ReadonlyUint8Array;
  isPrivate: boolean;
};

export type ChangeChannelSettingsInstructionDataArgs = { isPrivate: boolean };

export function getChangeChannelSettingsInstructionDataEncoder(): FixedSizeEncoder<ChangeChannelSettingsInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['isPrivate', getBooleanEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: CHANGE_CHANNEL_SETTINGS_DISCRIMINATOR,
    })
  );
}

export function getChangeChannelSettingsInstructionDataDecoder(): FixedSizeDecoder<ChangeChannelSettingsInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['isPrivate', getBooleanDecoder()],
  ]);
}

export function getChangeChannelSettingsInstructionDataCodec(): FixedSizeCodec<
  ChangeChannelSettingsInstructionDataArgs,
  ChangeChannelSettingsInstructionData
> {
  return combineCodec(
    getChangeChannelSettingsInstructionDataEncoder(),
    getChangeChannelSettingsInstructionDataDecoder()
  );
}

export type ChangeChannelSettingsAsyncInput<
  TAccountChannelAuthority extends string = string,
  TAccountChannel extends string = string,
  TAccountChannelMember extends string = string,
> = {
  channelAuthority: TransactionSigner<TAccountChannelAuthority>;
  channel: Address<TAccountChannel>;
  channelMember?: Address<TAccountChannelMember>;
  isPrivate: ChangeChannelSettingsInstructionDataArgs['isPrivate'];
};

export async function getChangeChannelSettingsInstructionAsync<
  TAccountChannelAuthority extends string,
  TAccountChannel extends string,
  TAccountChannelMember extends string,
  TProgramAddress extends Address = typeof SOLCHAT_PROGRAM_ADDRESS,
>(
  input: ChangeChannelSettingsAsyncInput<
    TAccountChannelAuthority,
    TAccountChannel,
    TAccountChannelMember
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  ChangeChannelSettingsInstruction<
    TProgramAddress,
    TAccountChannelAuthority,
    TAccountChannel,
    TAccountChannelMember
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLCHAT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    channelAuthority: {
      value: input.channelAuthority ?? null,
      isWritable: false,
    },
    channel: { value: input.channel ?? null, isWritable: true },
    channelMember: { value: input.channelMember ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.channelMember.value) {
    accounts.channelMember.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([95, 77, 69, 77, 66, 69, 82])),
        getAddressEncoder().encode(expectAddress(accounts.channel.value)),
        getAddressEncoder().encode(
          expectAddress(accounts.channelAuthority.value)
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.channelAuthority),
      getAccountMeta(accounts.channel),
      getAccountMeta(accounts.channelMember),
    ],
    data: getChangeChannelSettingsInstructionDataEncoder().encode(
      args as ChangeChannelSettingsInstructionDataArgs
    ),
    programAddress,
  } as ChangeChannelSettingsInstruction<
    TProgramAddress,
    TAccountChannelAuthority,
    TAccountChannel,
    TAccountChannelMember
  >);
}

export type ChangeChannelSettingsInput<
  TAccountChannelAuthority extends string = string,
  TAccountChannel extends string = string,
  TAccountChannelMember extends string = string,
> = {
  channelAuthority: TransactionSigner<TAccountChannelAuthority>;
  channel: Address<TAccountChannel>;
  channelMember: Address<TAccountChannelMember>;
  isPrivate: ChangeChannelSettingsInstructionDataArgs['isPrivate'];
};

export function getChangeChannelSettingsInstruction<
  TAccountChannelAuthority extends string,
  TAccountChannel extends string,
  TAccountChannelMember extends string,
  TProgramAddress extends Address = typeof SOLCHAT_PROGRAM_ADDRESS,
>(
  input: ChangeChannelSettingsInput<
    TAccountChannelAuthority,
    TAccountChannel,
    TAccountChannelMember
  >,
  config?: { programAddress?: TProgramAddress }
): ChangeChannelSettingsInstruction<
  TProgramAddress,
  TAccountChannelAuthority,
  TAccountChannel,
  TAccountChannelMember
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLCHAT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    channelAuthority: {
      value: input.channelAuthority ?? null,
      isWritable: false,
    },
    channel: { value: input.channel ?? null, isWritable: true },
    channelMember: { value: input.channelMember ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.channelAuthority),
      getAccountMeta(accounts.channel),
      getAccountMeta(accounts.channelMember),
    ],
    data: getChangeChannelSettingsInstructionDataEncoder().encode(
      args as ChangeChannelSettingsInstructionDataArgs
    ),
    programAddress,
  } as ChangeChannelSettingsInstruction<
    TProgramAddress,
    TAccountChannelAuthority,
    TAccountChannel,
    TAccountChannelMember
  >);
}

export type ParsedChangeChannelSettingsInstruction<
  TProgram extends string = typeof SOLCHAT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    channelAuthority: TAccountMetas[0];
    channel: TAccountMetas[1];
    channelMember: TAccountMetas[2];
  };
  data: ChangeChannelSettingsInstructionData;
};

export function parseChangeChannelSettingsInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedChangeChannelSettingsInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      channelAuthority: getNextAccount(),
      channel: getNextAccount(),
      channelMember: getNextAccount(),
    },
    data: getChangeChannelSettingsInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
