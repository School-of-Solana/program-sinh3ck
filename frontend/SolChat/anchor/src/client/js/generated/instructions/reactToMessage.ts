/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from 'gill';
import { SOLCHAT_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const REACT_TO_MESSAGE_DISCRIMINATOR = new Uint8Array([
  221, 59, 73, 208, 243, 164, 247, 43,
]);

export function getReactToMessageDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REACT_TO_MESSAGE_DISCRIMINATOR
  );
}

export type ReactToMessageInstruction<
  TProgram extends string = typeof SOLCHAT_PROGRAM_ADDRESS,
  TAccountAuthor extends string | AccountMeta<string> = string,
  TAccountParent extends string | AccountMeta<string> = string,
  TAccountMessageMetadata extends string | AccountMeta<string> = string,
  TAccountMessage extends string | AccountMeta<string> = string,
  TAccountChannelMember extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthor extends string
        ? WritableSignerAccount<TAccountAuthor> &
            AccountSignerMeta<TAccountAuthor>
        : TAccountAuthor,
      TAccountParent extends string
        ? ReadonlyAccount<TAccountParent>
        : TAccountParent,
      TAccountMessageMetadata extends string
        ? WritableAccount<TAccountMessageMetadata>
        : TAccountMessageMetadata,
      TAccountMessage extends string
        ? ReadonlyAccount<TAccountMessage>
        : TAccountMessage,
      TAccountChannelMember extends string
        ? ReadonlyAccount<TAccountChannelMember>
        : TAccountChannelMember,
      ...TRemainingAccounts,
    ]
  >;

export type ReactToMessageInstructionData = {
  discriminator: ReadonlyUint8Array;
  emoji: string;
  messageid: number;
};

export type ReactToMessageInstructionDataArgs = {
  emoji: string;
  messageid: number;
};

export function getReactToMessageInstructionDataEncoder(): Encoder<ReactToMessageInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['emoji', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ['messageid', getU32Encoder()],
    ]),
    (value) => ({ ...value, discriminator: REACT_TO_MESSAGE_DISCRIMINATOR })
  );
}

export function getReactToMessageInstructionDataDecoder(): Decoder<ReactToMessageInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['emoji', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ['messageid', getU32Decoder()],
  ]);
}

export function getReactToMessageInstructionDataCodec(): Codec<
  ReactToMessageInstructionDataArgs,
  ReactToMessageInstructionData
> {
  return combineCodec(
    getReactToMessageInstructionDataEncoder(),
    getReactToMessageInstructionDataDecoder()
  );
}

export type ReactToMessageAsyncInput<
  TAccountAuthor extends string = string,
  TAccountParent extends string = string,
  TAccountMessageMetadata extends string = string,
  TAccountMessage extends string = string,
  TAccountChannelMember extends string = string,
> = {
  author: TransactionSigner<TAccountAuthor>;
  parent: Address<TAccountParent>;
  messageMetadata?: Address<TAccountMessageMetadata>;
  message?: Address<TAccountMessage>;
  channelMember?: Address<TAccountChannelMember>;
  emoji: ReactToMessageInstructionDataArgs['emoji'];
  messageid: ReactToMessageInstructionDataArgs['messageid'];
};

export async function getReactToMessageInstructionAsync<
  TAccountAuthor extends string,
  TAccountParent extends string,
  TAccountMessageMetadata extends string,
  TAccountMessage extends string,
  TAccountChannelMember extends string,
  TProgramAddress extends Address = typeof SOLCHAT_PROGRAM_ADDRESS,
>(
  input: ReactToMessageAsyncInput<
    TAccountAuthor,
    TAccountParent,
    TAccountMessageMetadata,
    TAccountMessage,
    TAccountChannelMember
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  ReactToMessageInstruction<
    TProgramAddress,
    TAccountAuthor,
    TAccountParent,
    TAccountMessageMetadata,
    TAccountMessage,
    TAccountChannelMember
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLCHAT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    author: { value: input.author ?? null, isWritable: true },
    parent: { value: input.parent ?? null, isWritable: false },
    messageMetadata: { value: input.messageMetadata ?? null, isWritable: true },
    message: { value: input.message ?? null, isWritable: false },
    channelMember: { value: input.channelMember ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.messageMetadata.value) {
    accounts.messageMetadata.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([95, 77, 67])),
        getAddressEncoder().encode(expectAddress(accounts.parent.value)),
      ],
    });
  }
  if (!accounts.message.value) {
    accounts.message.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([95, 77, 69, 83, 83, 65, 71, 69])
        ),
        getAddressEncoder().encode(expectAddress(accounts.parent.value)),
        getU32Encoder().encode(expectSome(args.messageid)),
      ],
    });
  }
  if (!accounts.channelMember.value) {
    accounts.channelMember.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([95, 77, 69, 77, 66, 69, 82])),
        getAddressEncoder().encode(expectAddress(accounts.parent.value)),
        getAddressEncoder().encode(expectAddress(accounts.author.value)),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.author),
      getAccountMeta(accounts.parent),
      getAccountMeta(accounts.messageMetadata),
      getAccountMeta(accounts.message),
      getAccountMeta(accounts.channelMember),
    ],
    data: getReactToMessageInstructionDataEncoder().encode(
      args as ReactToMessageInstructionDataArgs
    ),
    programAddress,
  } as ReactToMessageInstruction<
    TProgramAddress,
    TAccountAuthor,
    TAccountParent,
    TAccountMessageMetadata,
    TAccountMessage,
    TAccountChannelMember
  >);
}

export type ReactToMessageInput<
  TAccountAuthor extends string = string,
  TAccountParent extends string = string,
  TAccountMessageMetadata extends string = string,
  TAccountMessage extends string = string,
  TAccountChannelMember extends string = string,
> = {
  author: TransactionSigner<TAccountAuthor>;
  parent: Address<TAccountParent>;
  messageMetadata: Address<TAccountMessageMetadata>;
  message: Address<TAccountMessage>;
  channelMember?: Address<TAccountChannelMember>;
  emoji: ReactToMessageInstructionDataArgs['emoji'];
  messageid: ReactToMessageInstructionDataArgs['messageid'];
};

export function getReactToMessageInstruction<
  TAccountAuthor extends string,
  TAccountParent extends string,
  TAccountMessageMetadata extends string,
  TAccountMessage extends string,
  TAccountChannelMember extends string,
  TProgramAddress extends Address = typeof SOLCHAT_PROGRAM_ADDRESS,
>(
  input: ReactToMessageInput<
    TAccountAuthor,
    TAccountParent,
    TAccountMessageMetadata,
    TAccountMessage,
    TAccountChannelMember
  >,
  config?: { programAddress?: TProgramAddress }
): ReactToMessageInstruction<
  TProgramAddress,
  TAccountAuthor,
  TAccountParent,
  TAccountMessageMetadata,
  TAccountMessage,
  TAccountChannelMember
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLCHAT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    author: { value: input.author ?? null, isWritable: true },
    parent: { value: input.parent ?? null, isWritable: false },
    messageMetadata: { value: input.messageMetadata ?? null, isWritable: true },
    message: { value: input.message ?? null, isWritable: false },
    channelMember: { value: input.channelMember ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.author),
      getAccountMeta(accounts.parent),
      getAccountMeta(accounts.messageMetadata),
      getAccountMeta(accounts.message),
      getAccountMeta(accounts.channelMember),
    ],
    data: getReactToMessageInstructionDataEncoder().encode(
      args as ReactToMessageInstructionDataArgs
    ),
    programAddress,
  } as ReactToMessageInstruction<
    TProgramAddress,
    TAccountAuthor,
    TAccountParent,
    TAccountMessageMetadata,
    TAccountMessage,
    TAccountChannelMember
  >);
}

export type ParsedReactToMessageInstruction<
  TProgram extends string = typeof SOLCHAT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    author: TAccountMetas[0];
    parent: TAccountMetas[1];
    messageMetadata: TAccountMetas[2];
    message: TAccountMetas[3];
    channelMember?: TAccountMetas[4] | undefined;
  };
  data: ReactToMessageInstructionData;
};

export function parseReactToMessageInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedReactToMessageInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === SOLCHAT_PROGRAM_ADDRESS
      ? undefined
      : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      author: getNextAccount(),
      parent: getNextAccount(),
      messageMetadata: getNextAccount(),
      message: getNextAccount(),
      channelMember: getNextOptionalAccount(),
    },
    data: getReactToMessageInstructionDataDecoder().decode(instruction.data),
  };
}
