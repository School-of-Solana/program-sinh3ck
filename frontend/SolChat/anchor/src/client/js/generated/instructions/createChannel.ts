/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from 'gill';
import { SOLCHAT_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const CREATE_CHANNEL_DISCRIMINATOR = new Uint8Array([
  37, 105, 253, 99, 87, 46, 223, 20,
]);

export function getCreateChannelDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CREATE_CHANNEL_DISCRIMINATOR
  );
}

export type CreateChannelInstruction<
  TProgram extends string = typeof SOLCHAT_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountChannel extends string | AccountMeta<string> = string,
  TAccountChannelMembers extends string | AccountMeta<string> = string,
  TAccountMessageMetadata extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountChannel extends string
        ? WritableAccount<TAccountChannel>
        : TAccountChannel,
      TAccountChannelMembers extends string
        ? WritableAccount<TAccountChannelMembers>
        : TAccountChannelMembers,
      TAccountMessageMetadata extends string
        ? WritableAccount<TAccountMessageMetadata>
        : TAccountMessageMetadata,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CreateChannelInstructionData = {
  discriminator: ReadonlyUint8Array;
  name: string;
  isPrivate: boolean;
};

export type CreateChannelInstructionDataArgs = {
  name: string;
  isPrivate: boolean;
};

export function getCreateChannelInstructionDataEncoder(): Encoder<CreateChannelInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['name', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ['isPrivate', getBooleanEncoder()],
    ]),
    (value) => ({ ...value, discriminator: CREATE_CHANNEL_DISCRIMINATOR })
  );
}

export function getCreateChannelInstructionDataDecoder(): Decoder<CreateChannelInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['name', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ['isPrivate', getBooleanDecoder()],
  ]);
}

export function getCreateChannelInstructionDataCodec(): Codec<
  CreateChannelInstructionDataArgs,
  CreateChannelInstructionData
> {
  return combineCodec(
    getCreateChannelInstructionDataEncoder(),
    getCreateChannelInstructionDataDecoder()
  );
}

export type CreateChannelAsyncInput<
  TAccountAuthority extends string = string,
  TAccountChannel extends string = string,
  TAccountChannelMembers extends string = string,
  TAccountMessageMetadata extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  channel?: Address<TAccountChannel>;
  channelMembers?: Address<TAccountChannelMembers>;
  messageMetadata?: Address<TAccountMessageMetadata>;
  systemProgram?: Address<TAccountSystemProgram>;
  name: CreateChannelInstructionDataArgs['name'];
  isPrivate: CreateChannelInstructionDataArgs['isPrivate'];
};

export async function getCreateChannelInstructionAsync<
  TAccountAuthority extends string,
  TAccountChannel extends string,
  TAccountChannelMembers extends string,
  TAccountMessageMetadata extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof SOLCHAT_PROGRAM_ADDRESS,
>(
  input: CreateChannelAsyncInput<
    TAccountAuthority,
    TAccountChannel,
    TAccountChannelMembers,
    TAccountMessageMetadata,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  CreateChannelInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountChannel,
    TAccountChannelMembers,
    TAccountMessageMetadata,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLCHAT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    channel: { value: input.channel ?? null, isWritable: true },
    channelMembers: { value: input.channelMembers ?? null, isWritable: true },
    messageMetadata: { value: input.messageMetadata ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.channel.value) {
    accounts.channel.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([95, 67, 72, 65, 78, 78, 69, 76])
        ),
        getUtf8Encoder().encode(
          expectSome(args.name)
        ),
      ],
    });
  }
  if (!accounts.channelMembers.value) {
    accounts.channelMembers.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([95, 77, 69, 77, 66, 69, 82])),
        getAddressEncoder().encode(expectAddress(accounts.channel.value)),
        getAddressEncoder().encode(expectAddress(accounts.authority.value)),
      ],
    });
  }
  if (!accounts.messageMetadata.value) {
    accounts.messageMetadata.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([95, 77, 67])),
        getAddressEncoder().encode(expectAddress(accounts.channel.value)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.channel),
      getAccountMeta(accounts.channelMembers),
      getAccountMeta(accounts.messageMetadata),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCreateChannelInstructionDataEncoder().encode(
      args as CreateChannelInstructionDataArgs
    ),
    programAddress,
  } as CreateChannelInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountChannel,
    TAccountChannelMembers,
    TAccountMessageMetadata,
    TAccountSystemProgram
  >);
}

export type CreateChannelInput<
  TAccountAuthority extends string = string,
  TAccountChannel extends string = string,
  TAccountChannelMembers extends string = string,
  TAccountMessageMetadata extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  channel: Address<TAccountChannel>;
  channelMembers: Address<TAccountChannelMembers>;
  messageMetadata: Address<TAccountMessageMetadata>;
  systemProgram?: Address<TAccountSystemProgram>;
  name: CreateChannelInstructionDataArgs['name'];
  isPrivate: CreateChannelInstructionDataArgs['isPrivate'];
};

export function getCreateChannelInstruction<
  TAccountAuthority extends string,
  TAccountChannel extends string,
  TAccountChannelMembers extends string,
  TAccountMessageMetadata extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof SOLCHAT_PROGRAM_ADDRESS,
>(
  input: CreateChannelInput<
    TAccountAuthority,
    TAccountChannel,
    TAccountChannelMembers,
    TAccountMessageMetadata,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): CreateChannelInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountChannel,
  TAccountChannelMembers,
  TAccountMessageMetadata,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLCHAT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    channel: { value: input.channel ?? null, isWritable: true },
    channelMembers: { value: input.channelMembers ?? null, isWritable: true },
    messageMetadata: { value: input.messageMetadata ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.channel),
      getAccountMeta(accounts.channelMembers),
      getAccountMeta(accounts.messageMetadata),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCreateChannelInstructionDataEncoder().encode(
      args as CreateChannelInstructionDataArgs
    ),
    programAddress,
  } as CreateChannelInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountChannel,
    TAccountChannelMembers,
    TAccountMessageMetadata,
    TAccountSystemProgram
  >);
}

export type ParsedCreateChannelInstruction<
  TProgram extends string = typeof SOLCHAT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    channel: TAccountMetas[1];
    channelMembers: TAccountMetas[2];
    messageMetadata: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: CreateChannelInstructionData;
};

export function parseCreateChannelInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedCreateChannelInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      channel: getNextAccount(),
      channelMembers: getNextAccount(),
      messageMetadata: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCreateChannelInstructionDataDecoder().decode(instruction.data),
  };
}
