/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from 'gill';
import { SOLCHAT_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const CREATE_DM_DISCRIMINATOR = new Uint8Array([
  46, 5, 70, 190, 39, 119, 37, 200,
]);

export function getCreateDmDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(CREATE_DM_DISCRIMINATOR);
}

export type CreateDmInstruction<
  TProgram extends string = typeof SOLCHAT_PROGRAM_ADDRESS,
  TAccountUserA extends string | AccountMeta<string> = string,
  TAccountUserB extends string | AccountMeta<string> = string,
  TAccountUserAProfile extends string | AccountMeta<string> = string,
  TAccountUserBProfile extends string | AccountMeta<string> = string,
  TAccountDmThread extends string | AccountMeta<string> = string,
  TAccountMessageMetadata extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountUserA extends string
        ? WritableSignerAccount<TAccountUserA> &
            AccountSignerMeta<TAccountUserA>
        : TAccountUserA,
      TAccountUserB extends string
        ? ReadonlyAccount<TAccountUserB>
        : TAccountUserB,
      TAccountUserAProfile extends string
        ? ReadonlyAccount<TAccountUserAProfile>
        : TAccountUserAProfile,
      TAccountUserBProfile extends string
        ? ReadonlyAccount<TAccountUserBProfile>
        : TAccountUserBProfile,
      TAccountDmThread extends string
        ? WritableAccount<TAccountDmThread>
        : TAccountDmThread,
      TAccountMessageMetadata extends string
        ? WritableAccount<TAccountMessageMetadata>
        : TAccountMessageMetadata,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CreateDmInstructionData = {
  discriminator: ReadonlyUint8Array;
  first: Address;
  second: Address;
};

export type CreateDmInstructionDataArgs = { first: Address; second: Address };

export function getCreateDmInstructionDataEncoder(): FixedSizeEncoder<CreateDmInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['first', getAddressEncoder()],
      ['second', getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: CREATE_DM_DISCRIMINATOR })
  );
}

export function getCreateDmInstructionDataDecoder(): FixedSizeDecoder<CreateDmInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['first', getAddressDecoder()],
    ['second', getAddressDecoder()],
  ]);
}

export function getCreateDmInstructionDataCodec(): FixedSizeCodec<
  CreateDmInstructionDataArgs,
  CreateDmInstructionData
> {
  return combineCodec(
    getCreateDmInstructionDataEncoder(),
    getCreateDmInstructionDataDecoder()
  );
}

export type CreateDmAsyncInput<
  TAccountUserA extends string = string,
  TAccountUserB extends string = string,
  TAccountUserAProfile extends string = string,
  TAccountUserBProfile extends string = string,
  TAccountDmThread extends string = string,
  TAccountMessageMetadata extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  userA: TransactionSigner<TAccountUserA>;
  userB: Address<TAccountUserB>;
  userAProfile?: Address<TAccountUserAProfile>;
  userBProfile?: Address<TAccountUserBProfile>;
  dmThread?: Address<TAccountDmThread>;
  messageMetadata?: Address<TAccountMessageMetadata>;
  systemProgram?: Address<TAccountSystemProgram>;
  first: CreateDmInstructionDataArgs['first'];
  second: CreateDmInstructionDataArgs['second'];
};

export async function getCreateDmInstructionAsync<
  TAccountUserA extends string,
  TAccountUserB extends string,
  TAccountUserAProfile extends string,
  TAccountUserBProfile extends string,
  TAccountDmThread extends string,
  TAccountMessageMetadata extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof SOLCHAT_PROGRAM_ADDRESS,
>(
  input: CreateDmAsyncInput<
    TAccountUserA,
    TAccountUserB,
    TAccountUserAProfile,
    TAccountUserBProfile,
    TAccountDmThread,
    TAccountMessageMetadata,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  CreateDmInstruction<
    TProgramAddress,
    TAccountUserA,
    TAccountUserB,
    TAccountUserAProfile,
    TAccountUserBProfile,
    TAccountDmThread,
    TAccountMessageMetadata,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLCHAT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    userA: { value: input.userA ?? null, isWritable: true },
    userB: { value: input.userB ?? null, isWritable: false },
    userAProfile: { value: input.userAProfile ?? null, isWritable: false },
    userBProfile: { value: input.userBProfile ?? null, isWritable: false },
    dmThread: { value: input.dmThread ?? null, isWritable: true },
    messageMetadata: { value: input.messageMetadata ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.userAProfile.value) {
    accounts.userAProfile.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([95, 85, 83, 69, 82])),
        getAddressEncoder().encode(expectAddress(accounts.userA.value)),
      ],
    });
  }
  if (!accounts.userBProfile.value) {
    accounts.userBProfile.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([95, 85, 83, 69, 82])),
        getAddressEncoder().encode(expectAddress(accounts.userB.value)),
      ],
    });
  }
  if (!accounts.dmThread.value) {
    accounts.dmThread.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([95, 68, 77])),
        getAddressEncoder().encode(expectSome(args.first)),
        getAddressEncoder().encode(expectSome(args.second)),
      ],
    });
  }
  if (!accounts.messageMetadata.value) {
    accounts.messageMetadata.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([95, 77, 67])),
        getAddressEncoder().encode(expectAddress(accounts.dmThread.value)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.userA),
      getAccountMeta(accounts.userB),
      getAccountMeta(accounts.userAProfile),
      getAccountMeta(accounts.userBProfile),
      getAccountMeta(accounts.dmThread),
      getAccountMeta(accounts.messageMetadata),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCreateDmInstructionDataEncoder().encode(
      args as CreateDmInstructionDataArgs
    ),
    programAddress,
  } as CreateDmInstruction<
    TProgramAddress,
    TAccountUserA,
    TAccountUserB,
    TAccountUserAProfile,
    TAccountUserBProfile,
    TAccountDmThread,
    TAccountMessageMetadata,
    TAccountSystemProgram
  >);
}

export type CreateDmInput<
  TAccountUserA extends string = string,
  TAccountUserB extends string = string,
  TAccountUserAProfile extends string = string,
  TAccountUserBProfile extends string = string,
  TAccountDmThread extends string = string,
  TAccountMessageMetadata extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  userA: TransactionSigner<TAccountUserA>;
  userB: Address<TAccountUserB>;
  userAProfile: Address<TAccountUserAProfile>;
  userBProfile: Address<TAccountUserBProfile>;
  dmThread: Address<TAccountDmThread>;
  messageMetadata: Address<TAccountMessageMetadata>;
  systemProgram?: Address<TAccountSystemProgram>;
  first: CreateDmInstructionDataArgs['first'];
  second: CreateDmInstructionDataArgs['second'];
};

export function getCreateDmInstruction<
  TAccountUserA extends string,
  TAccountUserB extends string,
  TAccountUserAProfile extends string,
  TAccountUserBProfile extends string,
  TAccountDmThread extends string,
  TAccountMessageMetadata extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof SOLCHAT_PROGRAM_ADDRESS,
>(
  input: CreateDmInput<
    TAccountUserA,
    TAccountUserB,
    TAccountUserAProfile,
    TAccountUserBProfile,
    TAccountDmThread,
    TAccountMessageMetadata,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): CreateDmInstruction<
  TProgramAddress,
  TAccountUserA,
  TAccountUserB,
  TAccountUserAProfile,
  TAccountUserBProfile,
  TAccountDmThread,
  TAccountMessageMetadata,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLCHAT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    userA: { value: input.userA ?? null, isWritable: true },
    userB: { value: input.userB ?? null, isWritable: false },
    userAProfile: { value: input.userAProfile ?? null, isWritable: false },
    userBProfile: { value: input.userBProfile ?? null, isWritable: false },
    dmThread: { value: input.dmThread ?? null, isWritable: true },
    messageMetadata: { value: input.messageMetadata ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.userA),
      getAccountMeta(accounts.userB),
      getAccountMeta(accounts.userAProfile),
      getAccountMeta(accounts.userBProfile),
      getAccountMeta(accounts.dmThread),
      getAccountMeta(accounts.messageMetadata),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCreateDmInstructionDataEncoder().encode(
      args as CreateDmInstructionDataArgs
    ),
    programAddress,
  } as CreateDmInstruction<
    TProgramAddress,
    TAccountUserA,
    TAccountUserB,
    TAccountUserAProfile,
    TAccountUserBProfile,
    TAccountDmThread,
    TAccountMessageMetadata,
    TAccountSystemProgram
  >);
}

export type ParsedCreateDmInstruction<
  TProgram extends string = typeof SOLCHAT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    userA: TAccountMetas[0];
    userB: TAccountMetas[1];
    userAProfile: TAccountMetas[2];
    userBProfile: TAccountMetas[3];
    dmThread: TAccountMetas[4];
    messageMetadata: TAccountMetas[5];
    systemProgram: TAccountMetas[6];
  };
  data: CreateDmInstructionData;
};

export function parseCreateDmInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedCreateDmInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 7) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      userA: getNextAccount(),
      userB: getNextAccount(),
      userAProfile: getNextAccount(),
      userBProfile: getNextAccount(),
      dmThread: getNextAccount(),
      messageMetadata: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCreateDmInstructionDataDecoder().decode(instruction.data),
  };
}
