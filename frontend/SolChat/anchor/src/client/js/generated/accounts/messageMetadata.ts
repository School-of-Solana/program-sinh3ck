/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU8Decoder,
  getU8Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from 'gill';

export const MESSAGE_METADATA_DISCRIMINATOR = new Uint8Array([
  99, 170, 51, 219, 112, 22, 245, 83,
]);

export function getMessageMetadataDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    MESSAGE_METADATA_DISCRIMINATOR
  );
}

export type MessageMetadata = {
  discriminator: ReadonlyUint8Array;
  count: number;
  emoji: string;
  bump: number;
};

export type MessageMetadataArgs = {
  count: number;
  emoji: string;
  bump: number;
};

/** Gets the encoder for {@link MessageMetadataArgs} account data. */
export function getMessageMetadataEncoder(): Encoder<MessageMetadataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['count', getU32Encoder()],
      ['emoji', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ['bump', getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: MESSAGE_METADATA_DISCRIMINATOR })
  );
}

/** Gets the decoder for {@link MessageMetadata} account data. */
export function getMessageMetadataDecoder(): Decoder<MessageMetadata> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['count', getU32Decoder()],
    ['emoji', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ['bump', getU8Decoder()],
  ]);
}

/** Gets the codec for {@link MessageMetadata} account data. */
export function getMessageMetadataCodec(): Codec<
  MessageMetadataArgs,
  MessageMetadata
> {
  return combineCodec(getMessageMetadataEncoder(), getMessageMetadataDecoder());
}

export function decodeMessageMetadata<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>
): Account<MessageMetadata, TAddress>;
export function decodeMessageMetadata<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>
): MaybeAccount<MessageMetadata, TAddress>;
export function decodeMessageMetadata<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>
):
  | Account<MessageMetadata, TAddress>
  | MaybeAccount<MessageMetadata, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getMessageMetadataDecoder()
  );
}

export async function fetchMessageMetadata<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<Account<MessageMetadata, TAddress>> {
  const maybeAccount = await fetchMaybeMessageMetadata(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeMessageMetadata<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<MaybeAccount<MessageMetadata, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeMessageMetadata(maybeAccount);
}

export async function fetchAllMessageMetadata(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<Account<MessageMetadata>[]> {
  const maybeAccounts = await fetchAllMaybeMessageMetadata(
    rpc,
    addresses,
    config
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeMessageMetadata(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<MaybeAccount<MessageMetadata>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeMessageMetadata(maybeAccount)
  );
}
