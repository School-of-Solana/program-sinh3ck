/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from 'gill';
import { SOLCHAT_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const JOIN_CHANNEL_DISCRIMINATOR = new Uint8Array([
  124, 39, 115, 89, 217, 26, 38, 29,
]);

export function getJoinChannelDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    JOIN_CHANNEL_DISCRIMINATOR
  );
}

export type JoinChannelInstruction<
  TProgram extends string = typeof SOLCHAT_PROGRAM_ADDRESS,
  TAccountUserAuthority extends string | AccountMeta<string> = string,
  TAccountUser extends string | AccountMeta<string> = string,
  TAccountChannel extends string | AccountMeta<string> = string,
  TAccountChannelMember extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountUserAuthority extends string
        ? WritableSignerAccount<TAccountUserAuthority> &
            AccountSignerMeta<TAccountUserAuthority>
        : TAccountUserAuthority,
      TAccountUser extends string
        ? WritableAccount<TAccountUser>
        : TAccountUser,
      TAccountChannel extends string
        ? WritableAccount<TAccountChannel>
        : TAccountChannel,
      TAccountChannelMember extends string
        ? WritableAccount<TAccountChannelMember>
        : TAccountChannelMember,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type JoinChannelInstructionData = { discriminator: ReadonlyUint8Array };

export type JoinChannelInstructionDataArgs = {};

export function getJoinChannelInstructionDataEncoder(): FixedSizeEncoder<JoinChannelInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: JOIN_CHANNEL_DISCRIMINATOR })
  );
}

export function getJoinChannelInstructionDataDecoder(): FixedSizeDecoder<JoinChannelInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getJoinChannelInstructionDataCodec(): FixedSizeCodec<
  JoinChannelInstructionDataArgs,
  JoinChannelInstructionData
> {
  return combineCodec(
    getJoinChannelInstructionDataEncoder(),
    getJoinChannelInstructionDataDecoder()
  );
}

export type JoinChannelAsyncInput<
  TAccountUserAuthority extends string = string,
  TAccountUser extends string = string,
  TAccountChannel extends string = string,
  TAccountChannelMember extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  userAuthority: TransactionSigner<TAccountUserAuthority>;
  user?: Address<TAccountUser>;
  channel: Address<TAccountChannel>;
  channelMember?: Address<TAccountChannelMember>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getJoinChannelInstructionAsync<
  TAccountUserAuthority extends string,
  TAccountUser extends string,
  TAccountChannel extends string,
  TAccountChannelMember extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof SOLCHAT_PROGRAM_ADDRESS,
>(
  input: JoinChannelAsyncInput<
    TAccountUserAuthority,
    TAccountUser,
    TAccountChannel,
    TAccountChannelMember,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  JoinChannelInstruction<
    TProgramAddress,
    TAccountUserAuthority,
    TAccountUser,
    TAccountChannel,
    TAccountChannelMember,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLCHAT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    userAuthority: { value: input.userAuthority ?? null, isWritable: true },
    user: { value: input.user ?? null, isWritable: true },
    channel: { value: input.channel ?? null, isWritable: true },
    channelMember: { value: input.channelMember ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.user.value) {
    accounts.user.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([95, 85, 83, 69, 82])),
        getAddressEncoder().encode(expectAddress(accounts.userAuthority.value)),
      ],
    });
  }
  if (!accounts.channelMember.value) {
    accounts.channelMember.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([95, 77, 69, 77, 66, 69, 82])),
        getAddressEncoder().encode(expectAddress(accounts.channel.value)),
        getAddressEncoder().encode(expectAddress(accounts.userAuthority.value)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.userAuthority),
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.channel),
      getAccountMeta(accounts.channelMember),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getJoinChannelInstructionDataEncoder().encode({}),
    programAddress,
  } as JoinChannelInstruction<
    TProgramAddress,
    TAccountUserAuthority,
    TAccountUser,
    TAccountChannel,
    TAccountChannelMember,
    TAccountSystemProgram
  >);
}

export type JoinChannelInput<
  TAccountUserAuthority extends string = string,
  TAccountUser extends string = string,
  TAccountChannel extends string = string,
  TAccountChannelMember extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  userAuthority: TransactionSigner<TAccountUserAuthority>;
  user: Address<TAccountUser>;
  channel: Address<TAccountChannel>;
  channelMember: Address<TAccountChannelMember>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getJoinChannelInstruction<
  TAccountUserAuthority extends string,
  TAccountUser extends string,
  TAccountChannel extends string,
  TAccountChannelMember extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof SOLCHAT_PROGRAM_ADDRESS,
>(
  input: JoinChannelInput<
    TAccountUserAuthority,
    TAccountUser,
    TAccountChannel,
    TAccountChannelMember,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): JoinChannelInstruction<
  TProgramAddress,
  TAccountUserAuthority,
  TAccountUser,
  TAccountChannel,
  TAccountChannelMember,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLCHAT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    userAuthority: { value: input.userAuthority ?? null, isWritable: true },
    user: { value: input.user ?? null, isWritable: true },
    channel: { value: input.channel ?? null, isWritable: true },
    channelMember: { value: input.channelMember ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.userAuthority),
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.channel),
      getAccountMeta(accounts.channelMember),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getJoinChannelInstructionDataEncoder().encode({}),
    programAddress,
  } as JoinChannelInstruction<
    TProgramAddress,
    TAccountUserAuthority,
    TAccountUser,
    TAccountChannel,
    TAccountChannelMember,
    TAccountSystemProgram
  >);
}

export type ParsedJoinChannelInstruction<
  TProgram extends string = typeof SOLCHAT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    userAuthority: TAccountMetas[0];
    user: TAccountMetas[1];
    channel: TAccountMetas[2];
    channelMember: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: JoinChannelInstructionData;
};

export function parseJoinChannelInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedJoinChannelInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      userAuthority: getNextAccount(),
      user: getNextAccount(),
      channel: getNextAccount(),
      channelMember: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getJoinChannelInstructionDataDecoder().decode(instruction.data),
  };
}
